.EnterPageBg
  .contain
    .container
      .row
        .col-md-6.enterDesc
          %h2 Make your choice with us
          %p At HOTEL BROWSER, we believe in the power of travel — and in helping you make the most of every trip. With over  million candid traveler reviews, we can help you make the right choice when you shop for hotels attractions.
        .col-md-6.registrationBlock
          %h2 Please Sign In
          = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
            .form-inputs
              = f.label :email
              %br/
              = f.email_field :email, required: false, autofocus: true, :class => 'form-control' , placeholder: "name@gmail.com"
              %br/
              = f.label :password
              %br/
              = f.password_field :password, required: false, :class => 'form-control' , placeholder: "password"
              %br/
              = f.check_box :remember_me
              = f.label :remember_me
            .form-actions
              = f.submit "Sign In", :class => 'btn btn-success'
              = render "devise/shared/links"
              = link_to 'Return on main page', home_index_path,:class => 'btn btn-light'